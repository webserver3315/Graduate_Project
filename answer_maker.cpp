
#include "bits/stdc++.h"
#include "answer_maker.h"
#define VERBOSE 0
using namespace std;

void test_routine();

int main(int argc, char** argv, char** env) {
    srand(time(NULL));
    printf("waveform file name is top.vcd\n");

    struct float32 alpha, bravo, acc;
    acc.total_int = 0;    
    
    test_routine();

    return 0;
}

